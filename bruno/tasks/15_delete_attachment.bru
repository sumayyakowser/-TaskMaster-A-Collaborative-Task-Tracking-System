meta {
  name: 15 Delete Attachment
  type: http
  seq: 15
}

delete {
  url: {{baseUrl}}/tasks/{{taskId}}/attachments/{{attachmentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
