meta {
  name: 07 Update Task
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}/tasks/{{taskId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Fix login bug (updated)",
    "description": "JWT token expires too early - investigating",
    "status": "IN_PROGRESS",
    "dueDate": "2026-07-25"
  }
}
